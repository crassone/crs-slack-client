# frozen_string_literal: true

RSpec.describe Crs::Slack::Client do
  it "has a version number" do
    expect(Crs::Slack::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
